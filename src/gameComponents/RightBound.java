package gameComponents;

import java.io.IOException;

public class RightBound extends BoundaryBlock {
  public RightBound( int x, int y, String rLocation, int tileW, int tileH ) throws IOException {
    super( x, y, rLocation, tileW, tileH );
  }
}