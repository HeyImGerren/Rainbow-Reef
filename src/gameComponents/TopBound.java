package gameComponents;

import java.io.IOException;

public class TopBound extends BoundaryBlock {
  public TopBound( int x, int y, String rLocation, int tileW, int tileH ) throws IOException {
    super( x, y, rLocation, tileW, tileH );
  }
}