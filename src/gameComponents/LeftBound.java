package gameComponents;

import java.io.IOException;

public class LeftBound extends BoundaryBlock {
  public LeftBound( int x, int y, String rLocation, int tileW, int tileH ) throws IOException {
    super( x, y, rLocation, tileW, tileH );
  }
}